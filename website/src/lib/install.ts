// The canonical brew install command. The landing page imports it and the
// getting-started doc carries the same text in its first ```bash fence
// (Experiment 6). Aspirational: the Homebrew tap/formula is not published yet.
// macOS-only is stated in the surrounding copy, not the command.
export const INSTALL = `brew install astrohacker/tap/termplot`;
